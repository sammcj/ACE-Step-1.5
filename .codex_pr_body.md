## Summary
Checkpoint review prompt for current branch state (`feat/decompose-api-next-step-20260301`) before the next decomposition slice.

Goal of this checkpoint:
- continue reducing `acestep/api_server.py` while preserving API/runtime behavior
- remove already-extracted duplicates and dead wrappers
- keep route contracts unchanged

## Scope
In scope for this checkpoint:
- deduplicate release-task primitives already extracted under `acestep/api/http/*`
- extract API-key auth into dedicated module
- remove wrapper/dead code in `api_server.py` where extracted modules already exist

Out of scope:
- API contract redesign
- model/runtime logic redesign
- non-API subsystems

## What Changed
- Added auth module:
  - `acestep/api/http/auth.py`
- Added auth tests:
  - `acestep/api/http/auth_test.py`
- Updated `acestep/api_server.py` to use extracted modules directly:
  - use `GenerateMusicRequest` from `release_task_models`
  - use `RequestParser` + parse helpers from `release_task_param_parser`
  - use `validate_audio_path` and `save_upload_to_temp` from `release_task_audio_paths`
  - remove dead model-download wrappers and thin runtime passthrough wrappers
  - keep route wiring behavior and response/auth semantics intact

`acestep/api_server.py` LOC reduction in this branch slice:
- from `1762` -> `1452`

## Validation
Executed targeted suites (all passed):
- `uv run python -m unittest -v acestep.api.http.release_task_models_test acestep.api.http.release_task_param_parser_test acestep.api.http.sample_format_routes_test`
- `uv run python -m unittest -v acestep.api.http.release_task_request_parser_test acestep.api.http.release_task_route_http_test`
- `uv run python -m unittest -v acestep.api.http.auth_test acestep.api.http.release_task_route_http_test acestep.api.http.query_result_route_http_test`
- `uv run python -m unittest -v acestep.api.http.release_task_audio_paths_test acestep.api.http.release_task_request_parser_test acestep.api.http.release_task_route_http_test acestep.api.http.query_result_route_http_test acestep.api.http.auth_test`
- `uv run python -m unittest -v acestep.api.train_api_service_http_test acestep.api.runtime_helpers_test acestep.api.http.release_task_route_http_test acestep.api.http.query_result_route_http_test`

## Reviewer Focus
Please prioritize behavioral parity over style:
1. Auth behavior parity:
   - `ai_token` body precedence
   - Authorization Bearer parsing
   - exact 401 error messages
2. `/release_task` parsing and upload-path safety behavior unchanged.
3. Training-route callback wiring still matches previous runtime behavior (tensorboard/jsonl/temp LLM model helpers).
4. No route response-shape/status regressions for `/release_task`, `/query_result`, and training status/stop endpoints.
